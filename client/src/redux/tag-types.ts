export enum tagTypes {
  user = "user",
  category = "category",
  product = "product",
  contact = "contact",
  orders = "orders",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.category,
  tagTypes.product,
  tagTypes.contact,
  tagTypes.orders,
];
