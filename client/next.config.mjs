/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "images.unsplash.com",
      "cdn.pixabay.com",
      "readymadeui.com",
    ],
  },
};

export default nextConfig;
